apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dashboard-agent.fullname" . }}-secret
  labels:
    {{- include "dashboard-agent.labels" . | nindent 4 }}
type: Opaque
data:
  DASHBOARD_TOKEN: {{ .Values.dashboard.token | b64enc | quote }}
