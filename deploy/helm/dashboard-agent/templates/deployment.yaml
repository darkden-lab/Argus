apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dashboard-agent.fullname" . }}
  labels:
    {{- include "dashboard-agent.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "dashboard-agent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "dashboard-agent.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "dashboard-agent.serviceAccountName" . }}
      containers:
        - name: agent
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: DASHBOARD_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "dashboard-agent.fullname" . }}-config
                  key: DASHBOARD_URL
            - name: CLUSTER_NAME
              valueFrom:
                configMapKeyRef:
                  name: {{ include "dashboard-agent.fullname" . }}-config
                  key: CLUSTER_NAME
            - name: DASHBOARD_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "dashboard-agent.fullname" . }}-secret
                  key: DASHBOARD_TOKEN
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8443
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8443
            initialDelaySeconds: 5
            periodSeconds: 10
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
