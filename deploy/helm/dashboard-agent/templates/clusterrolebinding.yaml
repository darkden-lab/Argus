apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "dashboard-agent.fullname" . }}
  labels:
    {{- include "dashboard-agent.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "dashboard-agent.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "dashboard-agent.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
