apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8s-dashboard.fullname" . }}-config
  labels:
    {{- include "k8s-dashboard.labels" . | nindent 4 }}
data:
  apiUrl: {{ .Values.frontend.env.apiUrl | quote }}
